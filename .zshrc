# setup fzf
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

# keybindings to get opt+arrow navigation
bindkey "^[^[[C" forward-word
bindkey "^[^[[D" backward-word

#########
# Aliases
#########

source ~/.bash_alias