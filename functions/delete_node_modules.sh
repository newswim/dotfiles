#!/bin/bash

# Exit immediately if any command returns a non-zero status
set -e

# Define the top-level folder where you want to start searching for node_modules folders
top_folder="."

# Use the find command to locate all node_modules folders and delete them
find "$top_folder" -type d -name "node_modules" -print -exec rm -rf {} \;

